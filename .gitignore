
# Playwright
node_modules/
# (keep Playwright caches and auth ignored)
/playwright/.cache/
/playwright/.auth/

# If you want to keep generated reports in source control, the following
# report directories were removed from ignore so they can be committed:
# - playwright-report/
allure-results/
allure-report/